#include <stdio.h>

/* Entrée : Tableau d'entiers et sa taille n
 * Sortie : Affichage du tableau    */

void print(int t[], int n) {
	int i ;

	for(i=0;i<n;i++) printf("%d",t[i]) ;
}

void appartient_grp_01(int x, int *T1, int s1, int n1) {
	int bol , i ;

	bol = 0 ;
	i = 0 ;
	while ((bol==0)&&(i<n1)) {
		if (x==*T1) bol = 1 ;
		i++ ;
		*T1++ ;
	}
	if ((bol==1)||(i<n)) printf("L'element %d appartient a l'ensemble",x) ;
	else printf("L'element %d n'appartient pas a l'ensemble",x) ;
}

void inclus_grp_01(int *T1, int *T2, int n1, int n2, int s1, int s2) {
	int i, j, compteur1, compteur2;
	
	for (i = 0; i < n1; i++) {
		for (j = 0; j < n2; j++){
			if (T[i] == T[j]) compteur1++;
		}	
	}
	for (i = 0; i < n2; i++) {
		for (j = 0; j < n1; j++){
			if (T[i] == T[j]) compteur2++;
		}	
	}
	if (compteur1 == n1) printf("Le tableau T1 est inclus dans le tableau T2");
	else if (compteur2 == n2) printf("Le tableau T2 est inclus dans le tableau T1");
	else printf("Aucun tableau n'est inclus dans l'autre");
}
